# Forced-prefix diversity

The diversity mechanism freezes deterministic three-ply prefixes, validates
their identity, and records exact source mixture metadata. It is intended for a
causal training comparison, not as an unbounded randomisation claim. Public
material contains the generator, validator and aggregate hashes; raw game
corpora and the generated prefix bank remain excluded.
